<!DOCTYPE html>
<html>
<head>
  <title><%= t '.title' %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= google_webfonts_init({
    google: ['Noto Sans', 'Lato', 'Material Icons']
  }) %>
</head>
<body>

<% unless @clear_template %>
  <div class="mass-container header">
    <h1>
      <%= t '.inventory_of', peer: t('.peer') %>
      <%= render 'trivia' %>
    </h1>
    <%= render 'layouts/nav' %>
  </div>
<% end %>

<div class="mass-container content <%= "clear" if @clear_template %>">
  <% flash.each do |name, msg| -%>
    <%= content_tag :div, msg, class: name %>
  <% end -%>
  <%= yield %>
</div>

<div class="footer <%= "clear" if @clear_template %>">
  © Roope Salmi 2015
  - <%= link_to t('.source_code'), 'https://github.com/ollpu/inventory/' %>
</div>

</body>
</html>
